#include "shell.h"

int own_cd(void)
{
printf("Working on it... \n");
return (0);
}

int own_env(void)
{
printf("Working on it ... \n");
return (0);
}

int own_help(void)
{
printf("Working on it ... \n");
return (0);
}

int own_exit(void)
{
printf("Working on it... \n");
return (0);
}
